apiVersion: v1
kind: Service
metadata:
  name: zapagendador-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: zapagendador
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}
